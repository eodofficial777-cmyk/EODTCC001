
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 開發期間暫時完全開放權限
    // 警告：這不安全，僅供開發使用。
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
